<?php

/**
 * ACF Custom Fields module settings
 */

// Exit if accessed directly.
if ( ! defined( 'ABSPATH' ) ) {
	exit;
}

// No module-specific fields; declared for consistency with other modules.
$frl_acf_default_fields = array();
